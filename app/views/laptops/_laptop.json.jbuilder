json.extract! laptop, :id, :model_number, :name, :serial_number, :ram, :hdd, :processor, :weight, :screen, :graphics_card, :usb_port, :hdmi_port, :available, :brand_id, :created_at, :updated_at
json.url laptop_url(laptop, format: :json)
